import react  from 'react';
import tasks from './tasks.js';
import fancytext from ',/fancytext.js';
import app from './app.css';

//motivational messages
if(task.isCompleted){
    ${'task.id'} ; {'task.name'} => isCompleted;
    motivationmessage="Great Job"
}
f(task.isCompleted){
    ${'task.id'} ; {'task.name'} => isPending;
    motivationmessage="keep going!"}

    //fancy text and button

    <div ClassName= 'Taskgenerator'>
        <Fancytext title ={title} text="Today's Tasks"
    <button onCLick={markascompleted}>Mark task as Completed</button>
    </div>

export default taskgenerator

const [currentindex, setindex]= useState(0);
const[index]= tasklist[task];
const nexttask = ()=>((taskindex+1)%tasklist.length);


